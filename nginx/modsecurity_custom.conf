SecAction \
 "id:900200,\
  phase:1,\
  nolog,\
  pass,\
  t:none,\
  setvar:'tx.allowed_methods=GET HEAD POST PUT DELETE OPTIONS PATCH'"

# Exclude LFI rule for calendar setup endpoint (profiles JSON key triggers false positive)
SecRule REQUEST_URI "@contains /calendar/setup" \
 "id:1000001,\
  phase:1,\
  nolog,\
  pass,\
  ctl:ruleRemoveById=930120"

# Exclude LFI rule for approvals setup endpoint (same issue with profiles)
SecRule REQUEST_URI "@contains /approvals/setup" \
 "id:1000002,\
  phase:1,\
  nolog,\
  pass,\
  ctl:ruleRemoveById=930120"

# Exclude LFI rule for auth setup endpoint
SecRule REQUEST_URI "@contains /auth/setup" \
 "id:1000003,\
  phase:1,\
  nolog,\
  pass,\
  ctl:ruleRemoveById=930120"
